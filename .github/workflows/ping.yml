name: Ping Nova App

on:
  schedule:
    - cron: '*/5 * * * *'  # Toutes les 5 minutes
  workflow_dispatch:        # Lancement manuel possible

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping l'app Nova
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://espace-partage-8.streamlit.app/
